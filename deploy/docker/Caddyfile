# Caddy: HTTPS tự động (Let's Encrypt) + reverse proxy vào app trong cùng compose.
# Đổi email + tên miền cho đúng của bạn.

{
	email admin@htqlqc.com
}

htqlqc.com, www.htqlqc.com {
	encode gzip
	reverse_proxy app:18088 {
		header_up X-Forwarded-Proto https
	}
}
